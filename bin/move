#!/usr/bin/env ruby

require_relative '../lib/move.rb'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

def move(board, index)
  puts "Where would you like to go?"
  input = gets. strip
  input_to_index
end

move(board, index)

display_board

